generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum AppStatus {
  Evaluated
  Applied
  Responded
  Interview
  Offer
  Rejected
  Discarded
  SKIP
}

model Application {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  date      String
  company   String
  role      String
  score     String
  status    AppStatus
  pdf       String
  reportName    String
  reportUrl String?
  url       String?  @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

/// A scan target (formerly a `tracked_companies` entry in portals.yml).
model Portal {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name       String   @unique
  careersUrl String?
  api        String?
  enabled    Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

/// Title-filter keywords (formerly `title_filter.positive` / `.negative`).
model FilterKeyword {
  id    String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  kind  String // "positive" | "negative"
  value String

  @@unique([kind, value])
}
